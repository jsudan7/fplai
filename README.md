# data-archive

Append-only archive of daily raw FPL API snapshots
(`bootstrap.json` + `fixtures.json`, gzipped per day) written by the
`model-run` GitHub Actions workflow via `scripts/archive_snapshot.sh`.
Orphan branch — no shared history with `main`.
